import { FC } from "react";
import { Wrapper } from "../../commons/components";

const Assign: FC = () => {
  return (
    <Wrapper>
      <h1>Assign Panel</h1>
    </Wrapper>
  );
};

export default Assign;
